通过 `mvn compile` 构建，使用 `mvn exec:java -Dexec.mainClass=...` 分别运行 `com.learnclaudecode.agents.S01AgentLoop`、`S04Subagent`、`S07TaskSystem`、`S11AutonomousAgentTeams`、`SFull` 等阶段入口；前端在 `web/` 目录下执行 `npm install && npm run dev` 启动。