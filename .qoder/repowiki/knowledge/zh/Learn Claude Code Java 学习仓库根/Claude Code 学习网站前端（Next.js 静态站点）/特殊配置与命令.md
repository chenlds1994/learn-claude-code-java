`npm run extract`（或 `npm run dev` / `npm run build` 的 pre 钩子）会运行 `scripts/extract-content.ts`，从仓库根 `agents/` 与 `docs/{en,zh,ja}/` 目录扫描 Python 源码和文档，生成 `src/data/generated/versions.json` 与 `docs.json`；若 `agents/` 不存在（如 Vercel 构建环境）则跳过并使用已提交的生成数据。