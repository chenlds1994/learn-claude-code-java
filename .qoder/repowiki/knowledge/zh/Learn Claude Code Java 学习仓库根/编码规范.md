- 每个教学阶段以独立的 `Sxx*` 入口类呈现，仅负责选择对应的 `StageConfig`，保持入口类薄而稳定
- 能力通过 `StageConfig` 声明式开关（工具、system prompt、高级机制）而非硬编码分支进行启用
- 外部知识以 `skills/<skill>/SKILL.md` 文件形式存放，由 `SkillLoader` 动态扫描加载，与代码解耦
- 配置集中从 `.env` 读取（`ANTHROPIC_API_KEY`、`MODEL_ID`、`ANTHROPIC_BASE_URL`、工作目录等），并通过 `EnvConfig` 暴露给各组件