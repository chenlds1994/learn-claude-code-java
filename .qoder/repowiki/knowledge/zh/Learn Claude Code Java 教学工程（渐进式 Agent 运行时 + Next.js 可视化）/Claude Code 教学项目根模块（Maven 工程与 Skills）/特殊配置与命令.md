通过 `mvn compile` 构建，使用 `mvn exec:java -Dexec.mainClass=...` 分别运行 `com.learnclaudecode.agents.S01AgentLoop`、`S06Subagent`、`S10TaskSystem`、`S13AgentTeams`、`SFull` 等阶段入口（s01~s17 共 17 个阶段入口 + SFull 全量入口）；测试通过 `mvn test` 执行（JUnit Jupiter 5.10.2 + maven-surefire-plugin 3.1.2）；前端在 `web/` 目录下执行 `npm install && npm run dev` 启动。