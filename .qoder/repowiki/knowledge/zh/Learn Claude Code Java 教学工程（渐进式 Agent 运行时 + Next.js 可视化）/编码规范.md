- 每个功能包在 src/test/java 下保持同名包结构并放置对应单元测试，如 tasks/ 对应 tasks/TaskManagerTest.java。
- Agent 能力以 S01~S17 及 SFull 命名的独立类逐步叠加，Main.java 默认委托 SFull 作为完整入口。
- 跨阶段共享的模型与工具集中在 model/、common/、tools/ 等公共包中，由多个 Stage 类复用而非各自重复实现。