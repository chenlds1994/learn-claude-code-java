/**
 * Root package for tests of the {@code com.learnclaudecode} learning harness.
 *
 * <p>Sub-packages mirror the production layout under {@code src/main/java}
 * (e.g. {@code agents}, {@code common}, {@code tools}) so that each test
 * class lives next to the code it exercises.</p>
 */
package com.learnclaudecode;
